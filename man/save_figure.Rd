% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{save_figure}
\alias{save_figure}
\title{Save a figure at print quality}
\usage{
save_figure(plot, path, width = 7, height = 5, dpi = 300)
}
\arguments{
\item{plot}{A ggplot object, such as any of the figures in this package.}

\item{path}{File to write. The extension chooses the device, so use \code{.pdf}
or \code{.eps} for vector output and \code{.png} or \code{.tiff} for raster.}

\item{width, height}{Figure size in inches.}

\item{dpi}{Resolution for raster devices.}
}
\value{
The normalised path, invisibly.
}
\description{
Thin wrapper over \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} with defaults suited to a
single-column journal figure: 7 by 5 inches at 300 dpi on a white
background. The output format follows the file extension. Missing parent
directories are created.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  grid <- data.frame(n_per_arm = c(500, 1000, 1500), power = c(0.3, 0.6, 0.8))
  p <- plot_power_curve(grid, x = "n_per_arm", y = "power")
  out <- save_figure(p, file.path(tempdir(), "power.png"))
  file.exists(out)
}
}
