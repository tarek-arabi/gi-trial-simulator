% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_power_curve}
\alias{plot_power_curve}
\title{Plot a performance measure against a design input}
\usage{
plot_power_curve(data, x, y, group = NULL)
}
\arguments{
\item{data}{A tidy data frame with one row per evaluated design, such as the
one \code{\link[=simulate_grid]{simulate_grid()}} returns.}

\item{x}{Name of the numeric column holding the design input, for example
\code{"n_per_arm_max"}.}

\item{y}{Name of the numeric column holding the performance measure. In the
output of \code{\link[=simulate_grid]{simulate_grid()}} power is reported as \code{"rejection_rate"}, with
its Monte Carlo standard error in \code{"rejection_rate_mcse"}.}

\item{group}{Optional name of a column to split the curves by. Coerced to a
factor and mapped to colour.}
}
\value{
A ggplot object with one line and point series per group, the
\code{gi_theme()} styling applied, axis labels taken from the column names, and
no plot title.
}
\description{
Draws power, or any other performance measure produced by \code{simulate_grid()},
against one design input, optionally split by a second one. If \code{data} carries
a Monte Carlo standard error for \code{y} in a column named \verb{<y>_mcse},
\verb{mcse_<y>} or \code{mcse}, a plus or minus 1.96 MCSE band is drawn behind the
line.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  grid <- data.frame(
    n_per_arm_max = rep(c(500, 1000, 1500, 2000), 2),
    rejection_rate = c(0.31, 0.55, 0.71, 0.82, 0.24, 0.44, 0.60, 0.72),
    rejection_rate_mcse = rep(0.015, 8),
    design_type = rep(c("Fixed", "Group sequential"), each = 4)
  )
  plot_power_curve(
    grid,
    x = "n_per_arm_max", y = "rejection_rate", group = "design_type"
  )
}
}
