% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prognostic.R
\name{prognostic_score}
\alias{prognostic_score}
\title{Score new patients with a frozen prognostic model}
\usage{
prognostic_score(fit, cohort)
}
\arguments{
\item{fit}{A \code{gi_prognostic} from \code{\link[=fit_prognostic]{fit_prognostic()}}.}

\item{cohort}{Data frame of covariates for the patients to be scored. Must
contain every predictor the model uses.}
}
\value{
Numeric vector of predicted linear predictors, one per row of
\code{cohort}.
}
\description{
Score new patients with a frozen prognostic model
}
\examples{
spec <- cohort_spec(list(covariate_spec("x1", "normal", mean = 0, sd = 1)))
train <- simulate_cohort(spec, n = 800, seed = 1)
y <- outcome_model(train, coefs = c(x1 = 1), intercept = -1, seed = 1)
fit <- fit_prognostic(train, y)
new <- simulate_cohort(spec, n = 5, seed = 2)
round(prognostic_score(fit, new), 3)
}
\seealso{
\code{\link[=fit_prognostic]{fit_prognostic()}}, \code{\link[=analyse_with_prognostic]{analyse_with_prognostic()}}
}
