% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_evsi}
\alias{plot_evsi}
\title{Plot an EVSI curve}
\usage{
plot_evsi(voi_curve_result)
}
\arguments{
\item{voi_curve_result}{The result of the package's EVSI curve routine: a
data frame, or a list containing one under \code{curve}, \code{evsi_curve}, \code{evsi},
\code{data} or \code{results}. The sample size column may be \code{n}, \code{n_total},
\code{n_per_arm} or \code{sample_size}; the value column \code{evsi}, \code{EVSI} or \code{value}.
The ribbon uses explicit \code{lower}/\code{upper} (or \code{ci_lower}/\code{ci_upper}) columns
when present, otherwise plus or minus 1.96 times a \code{se}, \code{mcse} or
\code{evsi_se} column, and is omitted when the curve reports neither.}
}
\value{
A ggplot object with the EVSI curve, its Monte Carlo ribbon where
available, the \code{gi_theme()} styling, and no plot title.
}
\description{
Expected value of sample information against the size of the trial being
costed, with a Monte Carlo uncertainty ribbon where the curve carries one.
}
\details{
Rows whose sample size or EVSI value is missing or non-finite cannot be
positioned, so they are dropped before plotting, as in \code{\link[=plot_boundaries]{plot_boundaries()}};
if that empties the curve the function stops. The ribbon is treated
separately: a row whose interval bounds are missing or non-finite keeps its
point on the curve and simply carries no ribbon there, and a curve whose
bounds are all unusable is drawn as a bare line. A negative standard error is
an error rather than a ribbon drawn upside down.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  voi <- list(curve = data.frame(
    n_per_arm = seq(200, 2000, by = 200),
    evsi = c(120, 210, 275, 320, 350, 368, 378, 383, 385, 386) * 1000,
    se = rep(9000, 10)
  ))
  plot_evsi(voi)
}
}
