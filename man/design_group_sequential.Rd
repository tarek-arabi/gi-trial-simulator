% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_gs.R
\name{design_group_sequential}
\alias{design_group_sequential}
\title{Size a group-sequential two-arm superiority trial}
\usage{
design_group_sequential(
  scenario,
  alpha = NULL,
  power = NULL,
  k = 3,
  type_of_design = "asOF",
  futility = c("none", "nonbinding_obf", "binding_obf"),
  information_rates = NULL,
  allocation_ratio = NULL
)
}
\arguments{
\item{scenario}{A \code{gi_scenario}, as returned by \code{\link[=scenario]{scenario()}}.}

\item{alpha}{One-sided type I error rate. Defaults to
\code{scenario$defaults$alpha}.}

\item{power}{Target power. Defaults to \code{scenario$defaults$power}.}

\item{k}{Number of analyses, including the final one. A single integer of
at least 2.}

\item{type_of_design}{One of \code{"asOF"} (O'Brien-Fleming-type alpha
spending, the default), \code{"asP"} (Pocock-type alpha spending), \code{"OF"}
(classical O'Brien-Fleming) or \code{"P"} (classical Pocock).}

\item{futility}{\code{"none"} for efficacy-only monitoring, \code{"nonbinding_obf"}
or \code{"binding_obf"} for O'Brien-Fleming-type beta spending
(rpact's \code{typeBetaSpending = "bsOF"}) with the corresponding
\code{bindingFutility} setting.}

\item{information_rates}{Optional numeric vector of length \code{k}, strictly
increasing and ending at 1. Defaults to equally spaced analyses.}

\item{allocation_ratio}{Planned ratio of treatment-arm to control-arm
sample size. Defaults to \code{scenario$defaults$allocation_ratio}.}
}
\value{
An object of class \code{gi_design} with \code{type = "group_sequential"}.
Beyond the shared contract, \code{detail} holds \code{efficacy_z}, \code{futility_z},
\code{information_rates}, \code{n_cumulative}, \code{cumulative_alpha_spent},
\code{expected_n_h0}, \code{expected_n_h1}, \code{expected_n_h01}, \code{k},
\code{type_of_design}, \code{futility}, \code{binding_futility}, the unrounded
\code{n_fixed}, the rounded \code{n_control} and \code{n_treatment}, \code{allocation_ratio},
\code{direction_upper}, and the underlying \code{rpact_design} and
\code{rpact_sample_size}.

\code{n_total} is the maximum sample size, always the sum of the two arm
sizes. \code{n_per_arm} is the common arm size and is defined only under 1:1
allocation; when \code{allocation_ratio} is anything else the arms differ and
\code{n_per_arm} is \code{NA_real_}, so that code which assumes balanced arms fails
rather than quietly using the wrong number. Read \code{detail$n_treatment} and
\code{detail$n_control} when allocation may be unequal.

\code{detail$futility_z} is \code{NA} at every analysis with no usable futility
bound, which includes the final analysis and any analysis where rpact
returns its no-futility floor of -6.
}
\description{
Builds the boundaries with \code{\link[rpact:getDesignGroupSequential]{rpact::getDesignGroupSequential()}} and the
sample size with \code{\link[rpact:getSampleSizeRates]{rpact::getSampleSizeRates()}}. Nothing about the
boundaries, the alpha spending or the expected sample sizes is computed
here.
}
\examples{
d <- design_group_sequential(scenario("ercp_acute_cholangitis"), k = 3)
d$n_total
gs_boundaries(d)

design_group_sequential(
  scenario("ercp_acute_cholangitis"),
  k = 2, futility = "nonbinding_obf"
)
}
\seealso{
\code{\link[=gs_boundaries]{gs_boundaries()}}, \code{\link[=design_fixed]{design_fixed()}}
}
