% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort.R
\name{covariate_spec}
\alias{covariate_spec}
\title{Describe the marginal distribution of one covariate}
\usage{
covariate_spec(name, dist, ...)
}
\arguments{
\item{name}{Covariate name. Becomes the column name in the simulated cohort.}

\item{dist}{One of \code{"normal"}, \code{"lognormal"}, \code{"gamma"}, \code{"binary"} or
\code{"ordinal"}.}

\item{...}{Distribution parameters. \code{normal} takes \code{mean} and \code{sd};
\code{lognormal} takes \code{meanlog} and \code{sdlog}; \code{gamma} takes \code{shape} and one of
\code{rate} or \code{scale}; \code{binary} takes \code{prob}; \code{ordinal} takes \code{probs} (a vector
of category probabilities summing to 1) and optionally \code{values} (the
numeric code assigned to each category, defaulting to \code{1:length(probs)}).}
}
\value{
An object of class \code{gi_covariate}: a list with elements \code{name},
\code{dist} and \code{pars} (the validated parameter list).
}
\description{
Describe the marginal distribution of one covariate
}
\examples{
covariate_spec("age", "normal", mean = 68, sd = 13)
covariate_spec("bilirubin", "lognormal", meanlog = 1.2, sdlog = 0.6)
covariate_spec("charlson", "ordinal", probs = c(0.4, 0.35, 0.25))
}
\seealso{
\code{\link[=cohort_spec]{cohort_spec()}}, \code{\link[=simulate_cohort]{simulate_cohort()}}
}
