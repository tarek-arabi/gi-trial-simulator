% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_fixed.R
\name{design_fixed}
\alias{design_fixed}
\alias{print.gi_design}
\title{Size a fixed-sample two-arm superiority trial}
\usage{
design_fixed(scenario, alpha = NULL, power = NULL, allocation_ratio = NULL)

\method{print}{gi_design}(x, ...)
}
\arguments{
\item{scenario}{A \code{gi_scenario}, as returned by \code{\link[=scenario]{scenario()}}.}

\item{alpha}{One-sided type I error rate. Defaults to
\code{scenario$defaults$alpha}.}

\item{power}{Target power. Defaults to \code{scenario$defaults$power}.}

\item{allocation_ratio}{Planned ratio of treatment-arm to control-arm
sample size. Defaults to \code{scenario$defaults$allocation_ratio}.}

\item{x}{A \code{gi_design}.}

\item{...}{Ignored, present for S3 consistency.}
}
\value{
An object of class \code{gi_design} with \code{type = "fixed"}. Beyond the
shared contract (\code{type}, \code{scenario}, \code{alpha}, \code{power}, \code{n_total},
\code{n_per_arm}, \code{engine}), \code{detail} holds \code{rpact_design},
\code{rpact_sample_size}, the unrounded \code{n_fixed}, the rounded \code{n_control} and
\code{n_treatment}, \code{allocation_ratio}, \code{direction_upper} and the z-scale
\code{critical_value}.

\code{x}, invisibly.
}
\description{
Delegates entirely to \code{rpact}: a one-stage group-sequential design supplies
the critical value, and \code{\link[rpact:getSampleSizeRates]{rpact::getSampleSizeRates()}} supplies the sample
size for the two binomial rates carried by the scenario.
}
\examples{
d <- design_fixed(scenario("ercp_acute_cholangitis"))
d$n_total
print(d)
}
\seealso{
\code{\link[=design_group_sequential]{design_group_sequential()}}, \code{\link[=power_at]{power_at()}}
}
