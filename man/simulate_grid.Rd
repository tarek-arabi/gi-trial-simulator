% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_grid}
\alias{simulate_grid}
\title{Simulate a grid of scenarios}
\usage{
simulate_grid(scenario_grid, design_fn, nsim = 10000, seed = 1, workers = 1)
}
\arguments{
\item{scenario_grid}{A \code{gi_scenario}, or a list of them. Names, if present,
are used to label rows.}

\item{design_fn}{Function of one argument (a \code{gi_scenario}) returning either
a \code{gi_design} or a single number giving the per-arm sample size of a fixed
design.}

\item{nsim}{Number of simulated trials per scenario.}

\item{seed}{Root seed from which the per-scenario streams are derived.}

\item{workers}{Number of parallel workers. Values above 1 use
\code{\link[parallel:mclapply]{parallel::mclapply()}} where the platform supports forking, and fall back
to serial evaluation on Windows.}
}
\value{
A data frame with one row per scenario: identifying columns, the
design used, and the ADEMP performance measures with their Monte Carlo
standard errors. The individual \code{gi_simulation} objects are attached as the
\code{"simulations"} attribute.
}
\description{
Builds a design for each scenario with \code{design_fn}, simulates it, and
returns one tidy row per scenario. Each scenario is given its own
L'Ecuyer-CMRG stream from \code{\link[=sim_seeds]{sim_seeds()}}, so the grid returns identical
numbers however many workers evaluate it.
}
\examples{
grid <- list(
  optimistic = scenario("ercp_acute_cholangitis",
    control_rate = 0.30, treatment_rate = 0.15
  ),
  null = scenario("ercp_acute_cholangitis",
    control_rate = 0.30, treatment_rate = 0.30
  )
)
simulate_grid(grid, design_fn = function(s) 161, nsim = 200, seed = 7)
}
\seealso{
\code{\link[=ademp_summary]{ademp_summary()}}, \code{\link[=sim_seeds]{sim_seeds()}}
}
