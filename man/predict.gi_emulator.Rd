% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emulator.R
\name{predict.gi_emulator}
\alias{predict.gi_emulator}
\title{Predict from a Gaussian process emulator}
\usage{
\method{predict}{gi_emulator}(object, newdata, include_nugget = FALSE, ...)
}
\arguments{
\item{object}{A \code{gi_emulator} from \code{\link[=fit_emulator]{fit_emulator()}}.}

\item{newdata}{Design points to predict at, on the same scale as the \code{x}
given to \code{\link[=fit_emulator]{fit_emulator()}}: a matrix with one row per point, a data frame,
a plain vector for a one input emulator, or a plain vector of length \code{d}
for a single point of a \code{d} input emulator. Columns are matched by name
when \code{newdata} names every training input, and by position otherwise.}

\item{include_nugget}{If \code{TRUE}, the returned standard deviation is that of a
new noisy simulator run at the point (latent uncertainty plus the nugget).
The default \code{FALSE} returns the uncertainty about the underlying smooth
response surface.}

\item{...}{Unused, present for compatibility with \code{\link[stats:predict]{stats::predict()}}.}
}
\value{
A data frame with one row per row of \code{newdata} and columns \code{mean},
\code{sd}, \code{lower} and \code{upper}, the last two being a 95 percent Gaussian
predictive interval.
}
\description{
Returns the posterior mean and the posterior standard deviation of the
emulated simulator output at new design points. The standard deviation is the
point of the exercise: it is how a user sees where the emulator is
interpolating between simulations and where it is guessing.
}
\note{
The interval describes the emulator's uncertainty about the simulator,
conditional on the fitted hyperparameters. It does not carry the
uncertainty in those hyperparameters, and it says nothing about whether the
simulator itself is a fair description of the trial.
}
\examples{
x <- matrix(seq(0, 1, length.out = 12), ncol = 1)
fit <- fit_emulator(x, sin(4 * x[, 1]))
predict(fit, newdata = c(0.05, 0.5, 0.95))
}
