% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_bayes.R
\name{posterior_prob_better}
\alias{posterior_prob_better}
\title{Posterior probability that treatment beats control}
\usage{
posterior_prob_better(
  events_t,
  n_t,
  events_c,
  n_c,
  prior = c(1, 1),
  direction = "lower_is_better",
  draws = 4000,
  seed = NULL
)
}
\arguments{
\item{events_t}{Number of events observed in the treatment arm.}

\item{n_t}{Number of participants observed in the treatment arm.}

\item{events_c}{Number of events observed in the control arm.}

\item{n_c}{Number of participants observed in the control arm.}

\item{prior}{Length-2 numeric vector \code{c(a0, b0)} giving the Beta prior shape
parameters, applied independently to both arms. Defaults to the uniform
prior \code{c(1, 1)}.}

\item{direction}{Either \code{"lower_is_better"} (treatment wins when its event
rate is lower, the usual case for mortality) or \code{"higher_is_better"}.}

\item{draws}{Number of Monte Carlo draws from each posterior.}

\item{seed}{Optional integer seed. When supplied, the generator kind is also
fixed, so the returned value depends on nothing but the arguments, and the
caller's random number state and generator kind are both restored on exit.
When \code{NULL} the ambient stream is used, which is what the internal
simulation loop needs in order to drive each replication from its own
independent stream.}
}
\value{
A single number in \verb{[0, 1]}: the posterior probability that the
treatment arm is better, in the sense given by \code{direction}.
}
\description{
With independent \code{Beta(a0, b0)} priors, the posterior for an arm observing
\code{events} responses out of \code{n} is \code{Beta(a0 + events, b0 + n - events)}. This
function reports the posterior probability that the treatment arm is the
better of the two, estimated by Monte Carlo draws from the two posteriors.
Which tail counts as better is set by \code{direction}, so an endpoint like
mortality is handled without the caller flipping any signs.
}
\details{
The estimate carries Monte Carlo error of at most \code{0.5 / sqrt(draws)}, so
4000 draws gives a standard error under 0.008 and 100000 draws under 0.0016.
Choose \code{draws} against the decision thresholds the value will be compared to.
When the value drives a stopping rule that error becomes a bias in the
decision rather than noise around it, which is why \code{\link[=design_bayesian]{design_bayesian()}}
enforces a minimum: see the \code{post_draws} entry there for the rule and for
what happens below it.
}
\examples{
posterior_prob_better(events_t = 5, n_t = 50, events_c = 12, n_c = 50, seed = 42)
posterior_prob_better(
  events_t = 5, n_t = 50, events_c = 12, n_c = 50,
  direction = "higher_is_better", seed = 42
)
}
