% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/voi.R
\name{population_evsi}
\alias{population_evsi}
\title{Scale per patient information value to a population}
\usage{
population_evsi(
  evsi_per_patient,
  incidence,
  horizon_years,
  discount_rate = 0.03
)
}
\arguments{
\item{evsi_per_patient}{Per patient expected value of sample information, as
returned in the \code{evsi} element of \code{\link[=evsi_trial]{evsi_trial()}}, in whatever unit the net
benefit function used.}

\item{incidence}{Number of patients per year facing the decision in the
population of interest.}

\item{horizon_years}{Number of years over which the trial result will inform
care.}

\item{discount_rate}{Annual discount rate as a proportion, defaulting to 0.03,
the conventional health economic rate. Use 0 for an undiscounted total.}
}
\value{
A list with \code{total} (the population value), \code{per_patient},
\code{effective_population} (the discounted patient count that \code{total} divides
by), \code{undiscounted_population}, the inputs \code{incidence}, \code{horizon_years} and
\code{discount_rate}, and \code{annual}, a data frame with one row per year giving
\code{year}, \code{patients}, \code{discount_factor}, \code{discounted_patients} and \code{value}.
}
\description{
A trial is run once and its answer is used on every patient who presents
afterwards, so the value of the trial is the per patient value multiplied by
the number of patients whose care it will inform, discounted because value
arriving in ten years is worth less than value arriving now.
}
\details{
The arithmetic is deliberately transparent: incidence is assumed constant
over the horizon, the first year is undiscounted, and year \code{t} is discounted
by \code{(1 + discount_rate)^-t} with \code{t} running from 0. Nothing here accounts
for the years the trial itself takes to report, during which the current
decision continues to be made in ignorance, nor for the possibility that the
answer is overtaken by a change in practice. Both shorten the effective
horizon, so this figure is an upper bound on the population value.
}
\examples{
population_evsi(evsi_per_patient = 0.0021, incidence = 45000,
                horizon_years = 10)
}
\seealso{
\code{\link[=evsi_trial]{evsi_trial()}}, \code{\link[=voi_curve]{voi_curve()}}
}
