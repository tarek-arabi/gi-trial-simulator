% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emulator.R
\name{fit_emulator}
\alias{fit_emulator}
\title{Fit a Gaussian process emulator to simulation output}
\usage{
fit_emulator(x, y, kernel = "matern52", nugget = NULL, lengthscale = NULL)
}
\arguments{
\item{x}{Design inputs: a numeric matrix with one row per simulated design
point and one column per input (for example control rate, effect size,
sample size per arm). A data frame or, for a single input, a plain numeric
vector is also accepted. At least \code{3 * (d + 2)} design points are required
for \code{d} inputs, three per hyperparameter, and about \code{10 * d} is the usual
working rule of thumb (Loeppky, Sacks and Welch 2009).}

\item{y}{Numeric vector of simulator output at those design points, one value
per row of \code{x} (for example simulated power).}

\item{kernel}{Correlation function, either \code{"matern52"} (the default, twice
differentiable and the usual choice for output from a stochastic simulator)
or \code{"squared_exponential"} (infinitely smooth, which can be optimistic).}

\item{nugget}{Non negative number added to the diagonal of the correlation
matrix, so it is expressed as a fraction of the signal variance, that is as
a noise to signal ratio. Use a small value such as \code{1e-10} for an
interpolating emulator of deterministic output, a larger one for output
carrying Monte Carlo noise. \code{NULL}, the default, estimates it.}

\item{lengthscale}{Correlation lengthscale on the scale of \code{x}, either one
number applied to every input or one per input. \code{NULL}, the default,
estimates them.}
}
\value{
An object of class \code{gi_emulator}, a list with elements \code{x}, \code{y}, \code{n},
\code{d}, \code{kernel}, \code{lengthscale} (named, on the scale of \code{x}), \code{nugget},
\code{sigma2} (signal variance on the standardised output scale), \code{loglik} (the
profile log marginal likelihood at the fit), \code{estimated} (which
hyperparameters were estimated rather than supplied), the standardisation
constants \code{x_center}, \code{x_scale}, \code{y_center}, \code{y_scale}, and the internal
quantities \code{x_scaled}, \code{lengthscale_scaled} (the lengthscales on the
standardised input scale, the scale the search box applies to), \code{chol} and
\code{alpha} used by the predict method.
}
\description{
Fits a zero mean Gaussian process to centred and scaled simulation output,
with either a Matern 5/2 or a squared exponential correlation function and a
separate lengthscale per input dimension. The signal variance is profiled out
of the log marginal likelihood analytically; the lengthscales and the nugget,
when not supplied, are estimated by maximising the exact profile log marginal
likelihood with \code{\link[stats:optim]{stats::optim()}} from several starting values.
}
\details{
Inputs are standardised internally (each column centred at its mean and
scaled by its standard deviation) and the transformation is recorded, so
\code{newdata} in \code{\link[=predict.gi_emulator]{predict.gi_emulator()}} and \code{bounds} in \code{\link[=suggest_next_point]{suggest_next_point()}}
are always given on the user's own scale. Lengthscales are reported on the
user's scale as well.
}
\note{
An emulator interpolates simulation output, it does not replace
simulation. Any operating characteristic quoted in a paper must come from
an actual simulation run at that design point, not from the emulator.
Because the process mean is fixed at the sample mean of \code{y} rather than
estimated jointly, predictive intervals far outside the design region are
mildly optimistic; they are also the region where the emulator should not
be trusted in the first place. The process is unconstrained, so a bounded
output such as power can be emulated as slightly above 1 or below 0 near
the boundary; emulate \code{qlogis(power)} and transform back if that matters.

Estimation is box constrained: on the standardised input scale the
lengthscales are searched over \verb{[1e-2, 1e3]} and the nugget over
\verb{[1e-10, 1]}. When the likelihood is still rising at one of those limits the
optimiser stops there and the value reported is the edge of the search
rather than an estimate, which is warned about, naming the hyperparameter
and the limit. A nugget at its upper limit or a lengthscale at its lower
limit means the output is dominated by noise and the emulator is fitting
essentially nothing; a lengthscale at its upper limit means that input is
inert across the design. A nugget resting at its lower limit is not warned
about, because that is the interpolating fit deterministic output should
give. A separate warning fires, for supplied as well as estimated
lengthscales, when the fitted correlation between the closest pair of design
points falls below 0.05, which leaves the posterior mean flat at the mean of
\code{y} between design points.
}
\examples{
x <- matrix(seq(0, 1, length.out = 10), ncol = 1)
y <- sin(3 * x[, 1]) + 0.2 * x[, 1]^2
fit <- fit_emulator(x, y)
fit$lengthscale
predict(fit, newdata = matrix(c(0.15, 0.45), ncol = 1))
}
\seealso{
\code{\link[=predict.gi_emulator]{predict.gi_emulator()}}, \code{\link[=emulator_loo]{emulator_loo()}}, \code{\link[=suggest_next_point]{suggest_next_point()}}
}
