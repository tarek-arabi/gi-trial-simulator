% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_bayes.R
\name{calibrate_bayesian}
\alias{calibrate_bayesian}
\title{Calibrate the efficacy threshold to a target type I error}
\usage{
calibrate_bayesian(
  scenario,
  n_max,
  target_alpha = 0.025,
  looks = 3,
  prior = c(1, 1),
  futility_threshold = 0.1,
  nsim = 5000,
  seed = 1,
  post_draws = 4000,
  workers = 1,
  tol = 1e-04,
  max_iter = 40
)
}
\arguments{
\item{scenario}{A \code{gi_scenario}, as returned by \code{\link[=scenario]{scenario()}}.}

\item{n_max}{Maximum total sample size across both arms.}

\item{target_alpha}{Target one-sided simulated type I error.}

\item{looks}{Number of analyses, including the final one.}

\item{prior}{Length-2 numeric vector \code{c(a0, b0)}, the Beta prior applied
independently to each arm.}

\item{futility_threshold}{Futility stopping threshold, held fixed during the
search.}

\item{nsim}{Number of simulated trials per hypothesis.}

\item{seed}{Integer seed, used for both the calibration run and the final
design, so the reported alpha of the design equals the achieved alpha of
the search.}

\item{post_draws}{Monte Carlo draws used for each posterior probability.}

\item{workers}{Number of forked processes used via \code{\link[parallel:mclapply]{parallel::mclapply()}}.}

\item{tol}{Width of the threshold bracket at which bisection stops.}

\item{max_iter}{Maximum number of bisection steps.}
}
\value{
A \code{gi_design} exactly as returned by \code{\link[=design_bayesian]{design_bayesian()}} at the
calibrated threshold, with \code{detail$calibration} added: the target and
achieved alpha, the Monte Carlo standard error of the achieved alpha, the
calibrated threshold, the final bracket, the number of iterations, and
whether the target was bracketed at all.
}
\description{
Searches for the posterior probability threshold at which the simulated type
I error of \code{\link[=design_bayesian]{design_bayesian()}} meets \code{target_alpha}. The null trajectories
are simulated once and rescored at every candidate threshold, so the search
compares like with like: using common random numbers makes simulated alpha a
monotone non-increasing step function of the threshold, which is what allows
plain bisection to be used.
}
\details{
The search returns the smallest bracketed threshold whose simulated type I
error does not exceed the target, so the achieved alpha is at or just below
\code{target_alpha} rather than straddling it.

Honest statement of what this gives you: the calibrated threshold is itself
a Monte Carlo estimate, not an exact constant. It is calibrated against one
particular set of \code{nsim} simulated null trials, and a different seed will
return a slightly different threshold. The achieved alpha is reported with
its Monte Carlo standard error, \code{sqrt(alpha * (1 - alpha) / nsim)}, which
falls only as \code{1 / sqrt(nsim)}: at \code{nsim = 5000} and a target of 0.025 the
standard error is about 0.0022, and reaching 0.0011 needs \code{nsim = 20000}.
Quadrupling \code{nsim} halves the uncertainty. The uncertainty in the threshold
itself is that error divided by the local slope of alpha against threshold,
so a design whose alpha changes slowly with the threshold will have a
correspondingly less precisely determined threshold. For a submission,
recalibrate at large \code{nsim} and confirm the threshold on an independent
seed.
}
\examples{
sc <- scenario("ercp_acute_cholangitis", control_rate = 0.30, treatment_rate = 0.15)
d <- calibrate_bayesian(sc, n_max = 200, looks = 1, nsim = 300, post_draws = 500, seed = 3)
d$detail$calibration$threshold
d$detail$calibration$achieved_alpha
}
\seealso{
\code{\link[=design_bayesian]{design_bayesian()}}
}
