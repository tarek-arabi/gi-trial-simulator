% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{benchmark_report}
\alias{benchmark_report}
\title{Summarise cross-engine agreement across a grid of designs}
\usage{
benchmark_report(
  k = 2:5,
  types = c("OF", "P", "asOF", "asP"),
  alpha = 0.025,
  beta = 0.1,
  tolerance = 1e-04
)
}
\arguments{
\item{k}{Integer vector of analysis counts to benchmark.}

\item{types}{Character vector of boundary families, drawn from \code{"OF"},
\code{"P"}, \code{"asOF"} and \code{"asP"}.}

\item{alpha}{One-sided type I error rate.}

\item{beta}{Type II error rate.}

\item{tolerance}{Absolute z-scale difference at or below which the two
engines are treated as agreeing.}
}
\value{
A data frame with one row per (\code{type}, \code{k}) combination and columns
\code{type}, \code{k}, \code{n_analyses}, \code{max_abs_diff}, \code{tolerance} and \code{all_agree}.
}
\description{
Runs \code{\link[=benchmark_gs_boundaries]{benchmark_gs_boundaries()}} over a grid of analysis counts and
boundary families and reduces each design to one row. This is the table the
README and the validation section of the manuscript cite.
}
\examples{
benchmark_report()
benchmark_report(k = 2:3, types = c("OF", "P"))
}
\seealso{
\code{\link[=benchmark_gs_boundaries]{benchmark_gs_boundaries()}}
}
