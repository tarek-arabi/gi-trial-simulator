% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort.R
\name{outcome_model}
\alias{outcome_model}
\title{Generate a binary outcome from cohort covariates}
\usage{
outcome_model(cohort, coefs, intercept, link = "logit", seed = 1)
}
\arguments{
\item{cohort}{A data frame of covariates, from \code{\link[=simulate_cohort]{simulate_cohort()}}.}

\item{coefs}{Named numeric vector of covariate coefficients on the link
scale. Names must be columns of \code{cohort}.}

\item{intercept}{Intercept on the link scale. Use \code{\link[=calibrate_intercept]{calibrate_intercept()}} to
choose one that reproduces a target marginal event rate.}

\item{link}{One of \code{"logit"}, \code{"probit"} or \code{"cloglog"}.}

\item{seed}{Integer seed for the Bernoulli draws, or \code{NULL} to use the
current RNG stream.}
}
\value{
An object of class \code{gi_outcome}: a list with \code{y} (the simulated 0/1
outcome), \code{p} (each patient's true event probability), \code{eta} (the linear
predictor), \code{event_rate} (the true marginal rate, \code{mean(p)}),
\code{observed_rate} (\code{mean(y)}), plus \code{intercept}, \code{coefs}, \code{link}, \code{n} and
\code{seed}.
}
\description{
Gives the virtual cohort an outcome with real, known covariate signal, so a
prognostic model has something to learn and the strength of that signal is
under the analyst's control.
}
\examples{
spec <- cohort_spec(list(covariate_spec("age", "normal", mean = 0, sd = 1)))
cohort <- simulate_cohort(spec, n = 500, seed = 3)
out <- outcome_model(cohort, coefs = c(age = 0.8), intercept = -2, seed = 3)
out$event_rate
}
\seealso{
\code{\link[=calibrate_intercept]{calibrate_intercept()}}, \code{\link[=fit_prognostic]{fit_prognostic()}}
}
