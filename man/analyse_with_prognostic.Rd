% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prognostic.R
\name{analyse_with_prognostic}
\alias{analyse_with_prognostic}
\title{Analyse a trial with and without prognostic adjustment}
\usage{
analyse_with_prognostic(
  outcome,
  arm,
  score,
  direction = c("lower_is_better", "higher_is_better")
)
}
\arguments{
\item{outcome}{Binary trial outcome: a 0/1 vector, a two-level factor, or a
\code{gi_outcome}.}

\item{arm}{Treatment assignment: 0 for control and 1 for treatment, or a
two-level factor whose second level is the treatment arm.}

\item{score}{Prognostic score, one value per patient, from
\code{\link[=prognostic_score]{prognostic_score()}}.}

\item{direction}{\code{"lower_is_better"} (the default, appropriate for mortality
and other harm endpoints) or \code{"higher_is_better"}.}
}
\value{
An object of class \code{gi_adjusted_analysis}: a list with \code{adjusted} and
\code{unadjusted}, each holding \code{estimate} (arm log odds ratio), \code{se}, \code{z} and
\code{p_one_sided}, plus \code{rd} (marginal risk difference), \code{rd_se}, \code{rd_z} and
\code{rd_p_one_sided}; then \code{se_ratio} (adjusted over unadjusted variance of the
risk difference, the variance-reduction factor), \code{se_ratio_logor} (the same
ratio for the log odds ratio, which normally exceeds 1), \code{z_ratio}, \code{n},
\code{n_treatment}, \code{n_events}, \code{direction}, \code{separation}, \code{rank_deficient} and
\code{converged}.

\code{converged} is \code{FALSE} when either \code{\link[stats:glm]{stats::glm()}} fit failed to converge,
when either shows complete or quasi-complete separation, or when either
design matrix was rank deficient. Each of the latter two raises a warning
and fills the corresponding element:
\itemize{
\item \code{separation} names the terms whose maximum likelihood estimate does not
exist because the outcome is perfectly predicted. \code{glm} reports
convergence in that state, so its own flag is not enough; detection here
is a conservative heuristic on the standardised coefficient and standard
error, not an exact test.
\item \code{rank_deficient} names the terms \code{glm} dropped because the design matrix
was not of full rank, the usual cause being a \code{score} that is a linear
function of \code{arm}. The g-computation risk difference is undefined for
such a fit, so \code{rd}, \code{rd_se}, \code{rd_z}, \code{rd_p_one_sided} and both
\code{se_ratio} values come back \code{NA}.
}
}
\description{
Fits the pre-specified adjusted model \code{outcome ~ arm + score} and, on the
same data, the unadjusted \code{outcome ~ arm}, so the two can be compared
directly.
}
\details{
Each model is summarised on two scales. The \strong{arm log odds ratio} is the
conditional effect the model estimates directly. The \strong{marginal risk
difference} is obtained by g-computation, predicting every patient under
both arms and averaging. The second scale exists because the logistic model
is non-collapsible: the adjusted and unadjusted log odds ratios are estimates
of two different quantities, so their standard errors cannot be compared,
whereas the risk difference is the same estimand either way and its standard
errors can.

The one-sided p value is computed in the tail implied by \code{direction}: for
\code{"lower_is_better"} evidence accumulates as the treatment log-odds and the
risk difference fall, so \code{p = pnorm(z)}.
}
\examples{
set.seed(1)
n <- 400
score <- stats::rnorm(n)
arm <- rep(0:1, each = n / 2)
y <- stats::rbinom(n, 1, stats::plogis(-1 + score - 0.4 * arm))
res <- analyse_with_prognostic(y, arm, score)
c(unadjusted = res$unadjusted$rd_se, adjusted = res$adjusted$rd_se)
}
\seealso{
\code{\link[=procova_gain]{procova_gain()}}
}
