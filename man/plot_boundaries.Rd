% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_boundaries}
\alias{plot_boundaries}
\title{Plot group-sequential stopping boundaries}
\usage{
plot_boundaries(design)
}
\arguments{
\item{design}{A \code{gi_design} of type \code{group_sequential}, or the data frame
\code{gs_boundaries()} returns, which is accepted directly so figures can be
redrawn from a stored table. Column names are resolved leniently: the
information fraction may be \code{information_rate}, \code{information_fraction} or
\code{timing}; the efficacy boundary \code{efficacy}, \code{efficacy_z} or \code{upper}; the
futility boundary \code{futility}, \code{futility_z} or \code{lower}. A design with no
futility column plots the efficacy boundary alone.}
}
\value{
A ggplot object with one line and point series per boundary, the
\code{gi_theme()} styling applied, and no plot title.
}
\description{
Efficacy and, where the design has them, futility boundaries on the z scale
against information fraction.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  bounds <- data.frame(
    analysis = 1:3,
    information_rate = c(1 / 3, 2 / 3, 1),
    efficacy = c(3.71, 2.51, 1.99),
    futility = c(-0.31, 0.98, 1.99)
  )
  plot_boundaries(bounds)
}
}
