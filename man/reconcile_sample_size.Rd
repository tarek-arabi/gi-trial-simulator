% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile.R
\name{reconcile_sample_size}
\alias{reconcile_sample_size}
\title{Reconcile a published sample size against every standard formulation}
\usage{
reconcile_sample_size(
  published_n_per_arm,
  p_control,
  p_treat,
  alpha_1sided,
  power,
  tolerance = 0.02
)
}
\arguments{
\item{published_n_per_arm}{The sample size per arm stated in the paper,
before any inflation for dropout.}

\item{p_control, p_treat}{The assumed event probabilities stated in the paper.}

\item{alpha_1sided}{One-sided type I error rate; halve a reported two-sided
alpha.}

\item{power}{Target power stated in the paper.}

\item{tolerance}{Relative agreement, as a proportion, within which a method
counts as reproducing the published number. Defaults to 0.02.}
}
\value{
A data frame with one row per method, ordered by absolute relative
difference: \code{method}, \code{n_per_arm}, \code{pct_vs_published} and \code{agrees}. The
attributed method, the achieved power at the published sample size, and
whether any method agreed are attached as the attributes \code{attribution},
\code{power_at_published} and \code{reproduced}.
}
\description{
Computes the per-arm sample size under each method in
\code{\link[=gi_n_reference]{gi_n_reference()}} and ranks them by closeness to the number the trial
published, so that a discrepancy between the engine and the publication can
be attributed to a named computational choice rather than left unexplained.
}
\details{
The attribution is descriptive. A published number matching the
continuity-corrected formula is evidence about how that trial was sized, not
evidence that either the trial or the engine is wrong.
}
\examples{
# Elmunzer 2012: published 474 per arm, reported as Fisher's exact
r <- reconcile_sample_size(474, 0.10, 0.05, 0.025, 0.80)
attr(r, "attribution")
}
\seealso{
\code{\link[=gi_n_reference]{gi_n_reference()}}, \code{\link[=benchmark_report]{benchmark_report()}}
}
