% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort.R
\name{simulate_cohort}
\alias{simulate_cohort}
\title{Simulate a virtual cohort by Gaussian copula}
\usage{
simulate_cohort(spec, n, seed = 1)
}
\arguments{
\item{spec}{A \code{gi_cohort_spec} from \code{\link[=cohort_spec]{cohort_spec()}}.}

\item{n}{Number of patients to simulate.}

\item{seed}{Integer seed passed to \code{\link[=set.seed]{set.seed()}}. Use \code{NULL} to draw from the
current RNG stream instead, which is what you want when calling this inside
a loop that has already set a seed for the replicate.}
}
\value{
A data frame with \code{n} rows and one column per covariate, carrying
attributes \code{seed} and \code{spec}.
}
\description{
Draws a latent multivariate normal with the specified correlation using a
Cholesky factor, maps it to uniforms with \code{\link[stats:pnorm]{stats::pnorm()}}, then through each
covariate's quantile function.
}
\details{
Known limitation, stated plainly: the correlation supplied in \code{\link[=cohort_spec]{cohort_spec()}}
is imposed on the \strong{latent normal} scale. For any non-normal marginal the
realised product-moment correlation of the observed variables is attenuated
towards zero relative to that target, severely so for binary marginals with
rates far from 0.5. This is a property of the Gaussian copula, not a bug, and
it is not corrected here. Use \code{\link[=cohort_correlation_check]{cohort_correlation_check()}} to see the size of
the attenuation for your own specification before relying on it.
}
\examples{
spec <- cohort_spec(
  list(
    covariate_spec("age", "normal", mean = 68, sd = 13),
    covariate_spec("sepsis", "binary", prob = 0.28)
  ),
  correlation = matrix(c(1, 0.3, 0.3, 1), nrow = 2)
)
head(simulate_cohort(spec, n = 5, seed = 42))
}
\seealso{
\code{\link[=cohort_spec]{cohort_spec()}}, \code{\link[=cohort_correlation_check]{cohort_correlation_check()}}, \code{\link[=outcome_model]{outcome_model()}}
}
