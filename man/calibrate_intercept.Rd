% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort.R
\name{calibrate_intercept}
\alias{calibrate_intercept}
\title{Find the intercept that yields a target marginal event rate}
\usage{
calibrate_intercept(cohort, coefs, target_rate, link = "logit")
}
\arguments{
\item{cohort}{A data frame of covariates, from \code{\link[=simulate_cohort]{simulate_cohort()}}.}

\item{coefs}{Named numeric vector of covariate coefficients on the link
scale.}

\item{target_rate}{Target marginal event rate, strictly between 0 and 1.}

\item{link}{One of \code{"logit"}, \code{"probit"} or \code{"cloglog"}.}
}
\value{
A single number: the intercept on the link scale.
}
\description{
Solves for the intercept, holding the covariate coefficients fixed, so that
the average predicted event probability over the cohort equals \code{target_rate}.
This is what lets a virtual cohort be calibrated to the control event rate a
parameter pack cites. The mean predicted probability is strictly increasing
in the intercept, so the root is unique.
}
\examples{
spec <- cohort_spec(list(covariate_spec("age", "normal", mean = 0, sd = 1)))
cohort <- simulate_cohort(spec, n = 5000, seed = 7)
a <- calibrate_intercept(cohort, coefs = c(age = 0.8), target_rate = 0.0658)
round(a, 3)
}
\seealso{
\code{\link[=outcome_model]{outcome_model()}}
}
