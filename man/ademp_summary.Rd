% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ademp.R
\name{ademp_summary}
\alias{ademp_summary}
\title{Performance measures for a simulation, with Monte Carlo standard errors}
\usage{
ademp_summary(sim, truth = NULL)
}
\arguments{
\item{sim}{A \code{gi_simulation}, as returned by \code{\link[=simulate_fixed]{simulate_fixed()}} or
\code{\link[=simulate_group_sequential]{simulate_group_sequential()}}.}

\item{truth}{True risk difference the estimator is judged against. Defaults
to the difference in the event rates the data were generated from.}
}
\value{
A data frame with one row per performance measure and columns
\code{measure}, \code{estimate}, \code{mcse} and \code{definition}. Rows are
\code{rejection_rate}, \code{bias}, \code{empirical_se}, \code{mse}, \code{coverage},
\code{mean_sample_size} and \code{degenerate_rate}, plus \code{early_stopping_rate} for a
group-sequential simulation.
}
\description{
Summarises a \code{gi_simulation} into the performance measures an ADEMP-compliant
report requires. Every measure carries a Monte Carlo standard error computed
with the formulas of Morris, White and Crowther (2019)
\url{doi:10.1002/sim.8086}, Table 6:
proportions use \code{sqrt(p (1 - p) / nsim)}; the mean and hence bias use the
empirical standard deviation divided by \code{sqrt(nsim)}; the empirical standard
error uses \code{empSE / sqrt(2 (nsim - 1))}; the mean squared error uses the
standard deviation of the squared errors divided by \code{sqrt(nsim)}.
}
\details{
The estimand is the risk difference, treatment event rate minus control event
rate, on its natural scale. For a group-sequential simulation the estimate is
the one available at the stopping analysis, so the bias row reports the
well-known bias of an estimate taken at a boundary crossing rather than an
error in the simulation.

A \code{degenerate_rate} row reports the replications whose score statistic or
confidence interval was undefined because an arm had no events or every
patient had one. Those replications are carried through the other measures
as non-rejections with a zero-width interval, which is what a real analysis
would face, so the row exists to make their weight visible: a rejection rate
resting on a large degenerate fraction is describing an infeasible trial, not
a design.
}
\examples{
sc <- scenario("ercp_acute_cholangitis", control_rate = 0.30, treatment_rate = 0.15)
sim <- simulate_fixed(sc, n_per_arm = 161, nsim = 500, seed = 1)
ademp_summary(sim)
}
\references{
Morris TP, White IR, Crowther MJ. Using simulation studies to
evaluate statistical methods. Statistics in Medicine. 2019;38(11):2074-2102.
\doi{10.1002/sim.8086}
}
\seealso{
\code{\link[=nsim_required]{nsim_required()}}, \code{\link[=ademp_skeleton]{ademp_skeleton()}}
}
