% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/voi.R
\name{voi_curve}
\alias{voi_curve}
\title{Expected value of sample information across trial sizes}
\usage{
voi_curve(prior_draws, n_grid, ...)
}
\arguments{
\item{prior_draws}{Draws of the treatment effect, as in \code{\link[=evsi_trial]{evsi_trial()}}.}

\item{n_grid}{Numeric vector of per arm sample sizes to evaluate.}

\item{...}{Passed to \code{\link[=evsi_trial]{evsi_trial()}}: \code{control_rate}, \code{net_benefit_fn},
\code{nsim} and \code{seed}.}
}
\value{
A data frame with one row per sample size and columns \code{n_per_arm},
\code{evsi}, \code{mcse}, \code{evsi_unpaired}, \code{ess_mean}, \code{evpi} and \code{fraction_of_evpi}.
The column names are chosen to be readable directly by \code{\link[=plot_evsi]{plot_evsi()}}.
}
\description{
Runs \code{\link[=evsi_trial]{evsi_trial()}} over a grid of per arm sample sizes and returns the curve
of information value against trial size. The shape is the point: EVSI rises
steeply while the trial is small, then flattens as the decision becomes
settled, and the sample size beyond which the curve is flat is buying
precision that changes no decision. The EVPI line is the asymptote the curve
can never cross.
}
\details{
Every point on the curve uses the same seed, so the simulated trials share
their parameter draws. That is a common random numbers design: it makes
differences between neighbouring sample sizes far less noisy than the
individual points, which is what the curve is read for.
}
\note{
The curve is a Monte Carlo estimate at every point, so it need not be
exactly monotone even though the quantity it estimates is, and once it
approaches saturation \code{fraction_of_evpi} can print slightly above 1. Both
are simulation noise: read the mcse column before reading a dip, or a
fraction above 1, as real.
}
\examples{
set.seed(1)
delta <- 0.0658 * (rlnorm(3000, meanlog = log(0.6), sdlog = 0.35) - 1)
nb <- function(d) c(standard_care = 0, early_ercp = -d - 0.005)
voi_curve(delta, n_grid = c(100, 400), control_rate = 0.0658,
          net_benefit_fn = nb, nsim = 100)
}
\seealso{
\code{\link[=evsi_trial]{evsi_trial()}}, \code{\link[=evpi]{evpi()}}, \code{\link[=population_evsi]{population_evsi()}}
}
